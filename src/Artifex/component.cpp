#include <Artifex/Engine.hpp>

namespace Artifex {

// size_t Engine::getComponentType(uuid_t id) { return componentType[id]; }

// bool Engine::setComponent(uuid_t entity, uuid_t component, const std::any &data) {
//   if (!m_entity.has(entity) || !m_entity[entity].data.has(component))
//     return false;
//
//   m_entity[entity].data[component].first = data;
//
//   return true;
// }

}; // namespace Artifex